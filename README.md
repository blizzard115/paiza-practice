# paiza-practice
paizaの問題をRubyで解いた記録です。

## Cランク

### 卵のペア問題

学んだこと

- `gets.split.map(&:to_i)` でスペース区切りの数字を受け取る
- `count` で配列の中にある数を数える
- 合計11になるペアは `1と10`, `2と9`, `3と8`, `4と7`, `5と6`
- `[count1, count2].min` で少ない方を取る

### 研究室の在室時間合計問題

学んだこと

- `"07:11".split(":")` で時刻を時間と分に分ける
- `hour * 60 + minute` で時刻を分に変換する
- 時刻の差分は「分」に直してから計算すると分かりやすい
- `/` で時間、`%` で余りの分を求める

### しりとり判定問題

#### 問題概要

N個の単語が順番に与えられる。  
前の単語の最後の文字と、次の単語の最初の文字がすべて一致していれば `Yes` を出力する。

途中で一致しない箇所があれば、初めて成立しなかった2文字を出力する。

#### 学んだこと

- `gets.chomp` で文字列を受け取る
- 配列に単語を保存する
- `words[i]` と `words[i + 1]` で隣同士の単語を比較する
- `word[0]` で最初の文字を取得する
- `word[-1]` で最後の文字を取得する
- `exit` で条件に合わなかった時点で処理を終了する

#### ポイント

最後の単語には次の単語がないため、比較するのは `0` から `n - 2` 番目まで。

```ruby
(0...n - 1).each do |i|

### クラス委員の投票集計問題

学んだこと

- `Hash.new(0)` で初期値0のHashを作る
- `votes[name] += 1` で名前ごとの得票数を数える
- `max_by` で一番大きい値を持つ要素を探す
- Hashは「名前 → 票数」のような対応関係を管理するときに便利

### 母音を取り除くハンドルネーム生成問題

学んだこと

- `each_char` で文字列を1文字ずつ処理する
- `include?` で配列に指定した文字が含まれるか判定する
- `result += char` で文字を連結する
- `unless` は「条件に当てはまらない場合に実行する」処理
- `gsub` を使うと文字列の置換・削除もできる

### エレベーター移動距離問題

学んだこと

- `current_floor` で現在地を管理する
- `next_floor` で次に止まった階を受け取る
- `(next_floor - current_floor).abs` で移動距離を求める
- 移動後は `current_floor = next_floor` で現在地を更新する

### ログ抽出問題

学んだこと

- `include?` で文字列の中に指定した文字列が含まれるか判定する
- 条件に合うログを配列に保存する
- `empty?` で配列が空かどうか確認する
- `puts 配列` で配列の要素を1行ずつ出力できる

### 人気メニュートップ10判定問題

学んだこと

- `total_calorie` で現在の合計カロリーを管理する
- `top10_count` で食べたトップ10メニュー数を数える
- `rank <= 10` でトップ10メニューか判定する
- `break` で条件を満たした時点でループを終了する
- `exit` で答えが確定した時点でプログラムを終了する

### ご近所さん距離判定問題

学んだこと

- `gets.split.map(&:to_i)` で座標を受け取る
- `(x1 - x2).abs + (y1 - y2).abs` で距離を計算する
- `abs` でマイナスをプラスに変換する
- 条件に合う件数を `count += 1` で数える

### パイザ記法の数式計算問題

学んだこと

- `split("+")` で文字列を区切る
- `count("<")` で特定の文字の数を数える
- `<` の数を10の位、`/` の数を1の位として数値に変換する
- `total += number` で合計を更新する

### 自動取引オークション問題

学んだこと

- `loop do` で終了条件が来るまで繰り返す
- 現在価格を `price` で管理する
- 最後に交渉できた人を `winner` で管理する
- 予算を超えたら `break` で処理を終了する
- 交互に処理する問題では、Aの処理・Bの処理を順番に書くと分かりやすい

### リースの回転一致判定問題

学んだこと

- 輪っか状の並びは「回転して一致するか」を考える
- `s1 + s1` にすると、s1の回転パターンがすべて含まれる
- `include?` で文字列の中に別の文字列が含まれるか判定できる
- 文字列の回転判定では `(s1 + s1).include?(s2)` が使える

### コーヒー割引累積問題

学んだこと

- `while x > 0` で値段が0円になるまで繰り返す
- `total += x` で支払った金額を合計する
- `x * (100 - p) / 100` でP%割引後の値段を計算する
- Rubyの整数同士の割り算では小数点以下が切り捨てられる

### 天秤の傾き判定問題

学んだこと

- `left` と `right` で左右の重さを管理する
- 操作の種類 `t` によって処理を分ける
- `D` が `"L"` か `"R"` かで更新する変数を変える
- おもりを移す操作では、片方から引いてもう片方に足す
- 各操作後に `left` と `right` を比較して出力する

### Leet文字列変換問題

学んだこと

- `tr` を使うと、複数の文字をまとめて置き換えられる
- `tr("AEGIOSZ", "4361052")` は、同じ位置の文字同士を対応させる
- Hashを使うと、文字ごとの変換ルールを自分で管理できる
- `each_char` で文字列を1文字ずつ処理できる

### どんぐり坂道エネルギー判定問題

学んだこと

- 下り坂ではエネルギーが増える
- 上り坂ではエネルギーが減る
- `energy` で現在残っている上れる高さを管理する
- エネルギーがマイナスになった時点で `NO` を出力する
- 最後まで進めたら `YES` を出力する

### 幸運な日カウント問題

学んだこと

- `0..364` で1年分の経過日数を調べる
- 数字に特定の数字列が含まれるか調べるときは文字列として扱う
- `to_s` で数値を文字列に変換する
- `include?` で文字列の中に指定した文字列が含まれるか判定する
- 条件に合う数を `count += 1` で数える

### N-しりとり判定問題

学んだこと

- `words[i]` と `words[i + 1]` で隣同士の単語を比較する
- `word[-n, n]` で末尾N文字を取得する
- `word[0, n]` で先頭N文字を取得する
- 1箇所でも一致しなければ `NO` を出して `exit` する
- 最後まで一致していれば `YES` を出力する

### デパートセール支払い金額計算問題

学んだこと

- `n >= x` でセール対象か判定する
- `prices.sum` で合計金額を求める
- `prices.sort` で価格を安い順に並べる
- `take(y)` で安い順にY個取り出す
- 合計金額から無料分を引いて支払額を求める

### テープ折り耐久性計算問題

学んだこと

- 配列を `tape[0...half]` と `tape[half...tape.length]` で半分に分ける
- `reverse` / `reverse!` で配列を反転できる
- 同じ位置同士の値を足して、新しい配列を作る
- 1回の操作ごとに `tape = new_tape` で状態を更新する
- `join(" ")` で配列を半角スペース区切りで出力する

### paicaバス支払いシミュレーション問題

学んだこと

- `money` でカード残高を管理する
- `point` で現在のポイントを管理する
- ポイントが運賃以上ならポイントを優先して使う
- カード残高で支払った場合のみ、運賃の10%がポイントとして増える
- ループの中で毎回の状態を更新して出力する

### 箱の回転数判定問題

学んだこと

- 文字列を回転させるには、先頭文字を後ろに移動する
- `s[1..-1]` で先頭以外の文字列を取り出せる
- `s[0]` で先頭の1文字を取り出せる
- `while s != t` で目標の文字列になるまで繰り返せる
- 回転回数を `count += 1` で数える

### ライブスケジュール判定問題

学んだこと

- `include?` で指定した日にライブがあるか確認する
- `1..31` で1ヶ月分の日付を順番に見る
- `has_a && has_b` でAとBのライブが被っているか判定する
- 被った日だけ `overlap_count` で回数を数える
- `even?` を使って A / B を交互に出力する

### 合言葉並び替え認証問題

学んだこと

- `chars` で文字列を1文字ずつの配列にできる
- `sort` で文字を並び替えられる
- 並び替えた結果を比較すると、同じ文字を使っているか判定できる
- `s != t` で完全一致を除外する
- `&&` で2つの条件を両方満たすか判定できる

### PAIZA 6 くじ一致数カウント問題

学んだこと

- `gets.split.map(&:to_i)` で1行の複数の数字を配列として受け取る
- `include?` で配列に数字が含まれているか確認する
- `each` でくじの数字を1つずつ確認する
- `match_count += 1` で一致した数を数える
- くじ1枚ごとに `match_count = 0` で数え直す

### スーパーのポイント計算問題

学んだこと

- `day.to_s.include?("5")` で日付に5が含まれるか判定する
- `elsif` を使って条件を順番に判定する
- `price * 5 / 100` で5%分のポイントを計算する
- Rubyの整数同士の割り算では小数点以下が切り捨てられる
- `total_point += ...` で合計ポイントを更新する

### 台風の暴風域判定問題

学んだこと

- 中心からの距離の2乗は `(x - xc) ** 2 + (y - yc) ** 2` で求める
- Rubyで2乗を書くときは `** 2`
- 平方根を使わず、距離の2乗と半径の2乗を比較できる
- `r1 ** 2 <= distance_squared && distance_squared <= r2 ** 2` で2つの円の間にいるか判定できる

### ストラックアウト得点集計問題

学んだこと

- 2次元のマス目は「行 i」と「列 j」で考える
- `states[i][j]` で i 行 j 列目の状態を確認できる
- `points[j]` で同じ列の得点を取得できる
- 状態が `"o"` の場所だけ得点を合計する
- `h.times` と `w.times` を組み合わせて全マスを確認する

### 複数人じゃんけん勝者判定問題

学んだこと

- `uniq` で配列から重複を取り除ける
- じゃんけんは人数ではなく、出ている手の種類で判定できる
- 手が1種類または3種類なら引き分け
- 手が2種類なら勝つ手を条件分岐で出力する
- `include?` で特定の手が出ているか確認できる
